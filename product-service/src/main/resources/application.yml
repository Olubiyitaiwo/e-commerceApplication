spring:
  application:
    name: product-service

  datasource:
    # url: jdbc:postgresql://localhost:5433/productdb
    url: jdbc:postgresql://ep-polished-scene-abrd6w19-pooler.eu-west-2.aws.neon.tech/productdb?sslmode=require
    username: product_user
    password: StrongPass123!
    driver-class-name: org.postgresql.Driver

  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8081
